{
  "name": "{{ appName }}",
  "version": "1.0.0",
  "subversion": {
    "toolkit": "{{ toolkitVersion }}"
  },
  "description": "",
  "scripts": {
    "build": "hap build",
    "release": "hap release",
    "server": "hap server",
    "watch": "hap watch"
  },
  "devDependencies": {
    "hap-toolkit":"^{{ toolkitVersion }}",
    "quickapp-dsls": "^0.1.1"
  }
}
